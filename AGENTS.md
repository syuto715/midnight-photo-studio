# AGENTS.md — 午前零時の写真館

## プロジェクト概要
「過去の写真」と「現在の部屋」を見比べ、写真に混入した嘘を〈場所+根拠〉の2段階で
指摘すると館の記録が修復される推理アドベンチャー。Web技術製。
最終ターゲットは Steam(Electron包装)とモバイル(Capacitor)。当面はブラウザで動けばよい。
企画の詳細は docs/design.md を参照。

## 技術スタック
- Vite + TypeScript(strict)。UIフレームワークは使わない(素のDOM + CSS transitions)。
  理由: 「押している間だけ切り替える」等の命令的演出が多く、
  Electron/Capacitor包装を軽く保つため。
- 外部ランタイム依存は原則ゼロ。ビルド成果物は静的ファイルのみ。
- 音は Web Audio 合成SEを正式採用。アセットファイルなしで控えめに鳴らす。

## 最重要アーキテクチャ原則
1. **コンテンツとエンジンの完全分離。** 部屋・写真・嘘・テキストはすべて
   `src/content/` 配下のデータ定義。「部屋を1つ増やす=データファイルを1つ足すだけ」を
   常に維持する。エンジン側に部屋固有のif文を書かない。
2. **差分はオブジェクトで表現する。** シーン=背景+配置オブジェクト(画像スプライト
   またはCSSシェイプ)。写真レイヤー=同じ背景+セピア/粒子フィルタ+独自のオブジェクト
   リスト。「写真と現在の違い」は必ずオブジェクトの有無・状態差として宣言的に定義する。
   画像に差分を焼き込まない(位置ズレと整合性事故を防ぐため)。
3. **画像はフォールバック前提。** 全オブジェクトは `image`(任意)と
   `fallbackShape`(色付き矩形+日本語ラベル)を持つ。画像が1枚も無くても常に完走できる。
   画像差し替えでコード変更ゼロ。
4. **テキストは全て辞書経由。** `src/i18n/ja.ts` を正とし、`en.ts` は同キーで用意
   (英訳の中身は後回し可)。コード内のハードコード文字列は禁止。
5. **ゲーム状態は単一ストア+イベント。** GameState(発見済み矛盾、修復済みの嘘、
   フラグ類)を1箇所に集約。演出はイベント購読で発火させる。
6. **セーブは localStorage、schemaVersion 付き。**(実装時期はプロンプト指示に従う)

## データモデル(概略)
- Room { id, background, layers: { now: SceneObject[], photo: SceneObject[] },
  anomalies: Anomaly[], lie: { spotId, reasonId }, hints: TextKey[] }
- SceneObject { id, pos, size, image?, fallbackShape, states?, inspectTextKey? }
- Anomaly { id, layer, spotId, memoTextKey }  // タップで矛盾メモに追加される違和感
- 嘘の指摘の正解判定は (spotId, reasonId) のペア一致で行う。

## UI原則
- ゲームステージは固定アスペクト 4:3。画面内で最大表示し、余白はレターボックス。
  矛盾メモ等のUIは下部ドロワー/オーバーレイ。これでスマホ縦・PC・Steam全画面すべて成立。
- 操作系(厳守): **長押し=反対レイヤーを一時表示(離すと戻る)/タップ=そのレイヤーの
  オブジェクトを調べる/ビュー切替はトグルボタン。** タップと長押しの閾値は約300ms。
  半透明オーバーレイでの重ね合わせ比較は実装しない(スマホで視認性が破綻する)。
- レイヤー切替は透過アニメなしの即時切替+微かなフィルムノイズ(パチッと変わるのが本体験)。
- タップ可能領域は最小44px。演出はCSS transition/keyframesで簡潔に、必ずスキップ可能に。

## デバッグ
- URLに `?debug=1` で: ヒットボックス可視化 / 任意状態へジャンプ / 正解表示 /
  矛盾メモ全開放。

## Verification Before Done(毎タスク必須)
1. `npm run typecheck` と `npm run build` が通る
2. `npm run dev` を起動し、以下を確認:
   - スマホ幅(375px)とPC幅の両方でレイアウト崩れなし
   - 長押しブリンクがタップと誤爆しない
   - 画像なし(フォールバックのみ)の状態で全フローを完走できる
3. 確認結果を完了報告に含める。未確認の完了報告は禁止。

## 進め方
- 大きな変更は必ず Plan Mode で計画→承認→実装。
- ハマりと学びは lessons.md に追記(特に iOS Safari のポインタイベント系)。
- スコープ外の思いつきは実装せず ideas.md にメモだけ残す。
